# Copy this file to .env and fill in your keys:
#   cp .env.template .env
#
# DO NOT commit .env to git.

# OpenAI (you have this)
OPENAI_API_KEY=sk-...

# Google AI Studio (free — aistudio.google.com → Get API Key)
GEMINI_API_KEY=...

# Groq (free — console.groq.com → API Keys)
GROQ_API_KEY=gsk_...

# Claude CLI mode (no Anthropic API key required):
# 1) install Claude Code CLI
# 2) run `claude auth login` once in your shell
# 3) use provider=claude_cli in config.MODELS
